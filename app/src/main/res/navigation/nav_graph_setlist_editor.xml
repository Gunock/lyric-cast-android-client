<?xml version="1.0" encoding="utf-8"?><!--
  ~ Created by Tomasz Kiljańczyk on 3/13/21 3:21 PM
  ~ Copyright (c) 2021 . All rights reserved.
  ~ Last modified 3/13/21 3:19 PM
  -->

<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph_setlist_editor"
    app:startDestination="@id/SetlistEditor">

    <fragment
        android:id="@+id/SetlistEditorSongs"
        android:name="pl.gunock.lyriccast.fragments.SetlistEditorSongsFragment"
        android:label="@string/label_songs"
        tools:layout="@layout/fragment_songs">

        <action
            android:id="@+id/action_SetlistEditorSongs_to_SetlistEditor"
            app:destination="@id/SetlistEditor" />
        <argument
            android:name="selectedSongs"
            app:argType="long[]" />
        <argument
            android:name="setlistName"
            app:argType="string" />
    </fragment>
    <fragment
        android:id="@+id/SetlistEditor"
        android:name="pl.gunock.lyriccast.fragments.SetlistEditorFragment"
        android:label="@string/label_songs"
        tools:layout="@layout/fragment_setlist_editor">

        <action
            android:id="@+id/action_SetlistEditor_to_SetlistEditorSongs"
            app:destination="@id/SetlistEditorSongs" />
        <argument
            android:name="selectedSongs"
            android:defaultValue="@null"
            app:argType="long[]"
            app:nullable="true" />
        <argument
            android:name="setlistName"
            android:defaultValue="@null"
            app:argType="string"
            app:nullable="true" />
    </fragment>
</navigation>